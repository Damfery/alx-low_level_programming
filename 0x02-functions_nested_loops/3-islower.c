#include "main.h"

/**
 * _islower - this prints all lower alphabet
 * 'c' - prints int
 * Return: 1 if c is lowercase, 0 (otherwise)
 */

int _islower(int c)

{
	c = _islower('c');
	_putchar(c + '0');
	return (0);
}
